# HW 9 Балансировка и отказоустойчивость

## Общая информация

### Архитектура

```
Client
  ↓
Nginx (Port 80) - Load Balancer для приложений
  ↓
├─→ web1:8000 ─┐
├─→ web2:8000 ─┼─→ HAProxy (5432, 5433) - Load Balancer для БД
└─→ web3:8000 ─┘        ↓
                  ├─→ Master (Write) - db:5432
                  └─→ Slaves (Read)
                       ├─→ slave1:5432
                       └─→ slave2:5432
```

### 1. HAProxy (social-network\haproxy\haproxy.cfg)

**Назначение:** Балансировка нагрузки на PostgreSQL

**Конфигурация:**
- **Алгоритм:** roundrobin (равномерное распределение)
- **Master:** `haproxy:5432` → `db:5432` (запись)
- **Slaves:** `haproxy:5433` → `slave1:5432`, `slave2:5432` (чтение)

**Параметры проверки здоровья:**
- `check inter 5s` - Проверка состояния каждые 5 секунд
- `fall 3` - После 3 неудачных проверок подряд → сервер DOWN (15 секунд)
- `rise 2` - После 2 успешных проверок подряд → сервер UP (10 секунд)

**Тип проверки:** Активный (HAProxy сам проверяет серверы)

---

### 2. Nginx (social-network\nginx\nginx.conf)

**Назначение:** Балансировка нагрузки на приложения

**Конфигурация:**
- **Алгоритм:** least_conn (наименьшее количество активных соединений)
- **Backends:** web1:8000, web2:8000, web3:8000

**Параметры проверки здоровья:**
- `max_fails=3` - После 3 неудачных запросов → сервер DOWN
- `fail_timeout=30s` - Период для подсчёта ошибок и время блокировки

**Тип проверки:** Пассивный (проверка на реальных запросах)

---

## Эксперимент

### Подготовка

**Запуск всех контейнеров:**
```bash
docker-compose up --build -d
```

**Проверка статуса:**
```bash
docker-compose ps
```

**Запуск нагрузочного теста:**
```bash
uv run python fault_tolerance_test.py
```

---

### Тест 1: Отказ PostgreSQL Slave

**Действие:**
```bash
docker kill postgres_slave1
```

**Результаты:**
1. В веб-интерфейсе HAProxy (http://localhost:8404/stats) slave1 перешел в статус **DOWN** 🔴
2. RPS временно упала
3. Было несколько ошибок в момент переключения
4. Все запросы на чтение автоматически переключились на slave2
5. Система продолжила работу без критических сбоев

**Вывод:** ✅ Автоматический failover работает, система отказоустойчива

---

### Тест 2: Отказ серверов приложений

**Действие:**
```bash
docker kill social-network-web2
docker kill social-network-web3
```

**Результаты:**
1. По логам контейнера определил, что вся нагрузка перешла на web1:
   ```bash
   docker logs social-network-web1 | tail -20
   ```
2. RPS упала (1 сервер вместо 3)
3. Система продолжила обрабатывать запросы
4. Nginx автоматически исключил недоступные серверы из балансировки

**Восстановление:**
```bash
docker-compose up -d web2 web3
```

5. После включения контейнеров нагрузка автоматически перераспределилась между всеми серверами
6. RPS вернулась к нормальным значениям

**Вывод:** ✅ Nginx корректно обрабатывает отказы, система масштабируется

---

## Мониторинг

### HAProxy Stats
**URL:** http://localhost:8404/stats

**Показывает:**
- Статус каждого backend (UP/DOWN)
- Количество запросов на каждый сервер
- Количество неудачных проверок
- Время последней проверки

### Nginx Status
**URL:** http://localhost/nginx_status

**Показывает:**
```
Active connections: 3
server accepts handled requests
 18 18 56
Reading: 0 Writing: 1 Waiting: 2
```

### Логи приложений
```bash
# Проверка распределения нагрузки
docker logs social-network-web1 | tail -5
docker logs social-network-web2 | tail -5
docker logs social-network-web3 | tail -5
```

## Конфигурационные файлы

### HAProxy
- Файл: `social-network/haproxy/haproxy.cfg`
- Порты: 5432 (master), 5433 (slaves), 8404 (stats)

### Nginx
- Файл: `social-network/nginx/nginx.conf`
- Порт: 80

### Docker Compose
- Файл: `social-network/docker-compose.yml`
- Сервисы: nginx, haproxy, web1, web2, web3, db, db-slave1, db-slave2






